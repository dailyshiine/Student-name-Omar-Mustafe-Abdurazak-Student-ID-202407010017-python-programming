W7 Documentation

Bug identified in main.py:
- The original code did not validate user input (could accept negative price/quantity or crash).
- Incorrectly called the function with total calculation instead of passing price and quantity separately.
- Poor input prompts and formatting.

Fixes applied:
- Created calculate_total() function in food_order.py with proper validation.
- Fixed import and function call in main.py.
- Added input validation to prevent invalid orders.
- Wrote comprehensive tests using pytest.

The program now handles invalid inputs gracefully by raising ValueError.
All tests pass.